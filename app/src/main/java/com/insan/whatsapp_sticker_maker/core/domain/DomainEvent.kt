package com.insan.whatsapp_sticker_maker.core.domain

/**
 * Marker interface for all domain events raised by aggregates. Dispatch via [EventBus] in the
 * Application layer.
 */
interface DomainEvent
