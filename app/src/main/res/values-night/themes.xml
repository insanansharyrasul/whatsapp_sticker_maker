<resources>
    <!-- Dark window theme. Compose's AppTheme handles all UI colour/typography. -->
    <style name="Theme.Whatsapp_sticker_maker" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>