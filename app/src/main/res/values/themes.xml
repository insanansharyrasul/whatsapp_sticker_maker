<resources>
    <!-- Minimal window theme. Compose's AppTheme handles all UI colour/typography. -->
    <style name="Theme.Whatsapp_sticker_maker" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>